# archon-web

El sitio de Archon (`archon-uae.com`). Un solo `index.html` autocontenido:
las imagenes van en base64 dentro del HTML, asi que no hay assets sueltos.

**No se edita aqui.** La fuente son las partes en
`archon-core/sites/archon-mise/src/` y se ensamblan con `build.py`.
